# ── Database ──────────────────────────────────────────────────────────────────────────────────
# Neon's pooled connection string. Locally, point this at a Neon branch rather than a local
# Postgres — the free tier includes 10 branches, so there is nothing to install.
# In production the Neon–Vercel integration sets this for you.
DATABASE_URL=
